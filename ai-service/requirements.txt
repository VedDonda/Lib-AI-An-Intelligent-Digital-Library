# Web framework
fastapi==0.115.12
uvicorn==0.34.2

# Config / utils
python-dotenv==1.1.0
requests==2.32.3
pydantic==2.11.1
pydantic-settings==2.9.1


# PDF parsing

pymupdf==1.25.5

# ── LangChain ecosystem ────────────────────────────────────────────────────────
# ALL packages MUST be pinned together. Without pins, pip backtracks through
# hundreds of versions and the build times out (as seen in the Render build log).
langchain==0.3.25
langchain-community==0.3.24
langchain-core==0.3.84
langchain-text-splitters==0.3.11
langsmith==0.3.45
langchain-google-genai==2.1.12
langchain-huggingface==0.1.2
langchain-qdrant==0.2.0

# Vector store client
qdrant-client==1.14.2

# Embeddings / ML
sentence-transformers==3.4.1